import { BadgeDollarSign, BookOpen, Megaphone, Utensils } from "lucide-react";
import Reveal from "./Reveal";
type Benefit = [typeof Utensils, string, string];
const items: Benefit[] = [[Utensils, "Cardápios que inspiram", "Novas combinações para renovar sua vitrine e seu menu."], [BookOpen, "Receitas prontas para testar", "Modo de preparo, rendimento e orientação para começar."], [BadgeDollarSign, "Mais clareza no preço", "Referências para pensar custo, margem e posicionamento."], [Megaphone, "Conteúdo para divulgar", "Ideias de comunicação para apresentar cada novidade."]];
export default function Benefits() { return <section id="benefícios" className="section benefits"><div className="container"><Reveal><p className="eyebrow">O apoio certo para a sua cozinha</p><h2>Do primeiro rascunho<br /><em>à próxima venda.</em></h2></Reveal><div className="benefit-grid">{items.map(([Icon, title, text], i) => { const I = Icon as typeof Utensils; return <Reveal key={title as string} delay={i * .08}><article className="benefit-card"><I size={27} strokeWidth={1.5} /><span className="card-index">0{i + 1}</span><h3>{title as string}</h3><p>{text as string}</p></article></Reveal>; })}</div></div></section>; }
