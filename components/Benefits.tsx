import { BadgeDollarSign, BookOpen, Megaphone, Utensils } from "lucide-react";
import Reveal from "./Reveal";
type Benefit = [typeof Utensils, string, string];
const items: Benefit[] = [[Utensils, "Cardápios mensais", "Novas ideias prontas para colocar no seu menu."], [BookOpen, "Receitas testadas", "Preparo claro, rendimento e padrão para sua operação."], [BadgeDollarSign, "Precificação inteligente", "Mais clareza para proteger sua margem."], [Megaphone, "Material de marketing", "Conteúdo para divulgar e vender seus produtos."]];
export default function Benefits() { return <section id="benefícios" className="section benefits"><div className="container"><Reveal><p className="eyebrow">Tudo em um só lugar</p><h2>Tudo o que você precisa<br /><em>para vender mais.</em></h2></Reveal><div className="benefit-grid">{items.map(([Icon, title, text], i) => { const I = Icon as typeof Utensils; return <Reveal key={title as string} delay={i * .08}><article className="benefit-card"><I size={27} strokeWidth={1.5} /><span className="card-index">0{i + 1}</span><h3>{title as string}</h3><p>{text as string}</p></article></Reveal>; })}</div></div></section>; }
