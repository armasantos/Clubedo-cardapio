"use client";
import { useEffect, useState } from "react";
import { Star } from "lucide-react";
import Reveal from "./Reveal";
const testimonials = [["MC", "Marina C.", "Entrei buscando ideias e encontrei um jeito mais claro de planejar o meu menu.", "Marmitas"], ["RA", "Rafael A.", "As fichas técnicas me ajudaram a colocar ordem no preparo e na rotina da equipe.", "Delivery"], ["JS", "Juliana S.", "O conteúdo me dá repertório sem perder de vista o que combina com os meus clientes.", "Confeitaria"], ["PL", "Paulo L.", "Hoje eu consigo olhar para o próximo mês com mais organização e menos improviso.", "Restaurante"], ["BS", "Bianca S.", "Gosto porque é bonito, direto e pensado para quem precisa colocar a mão na massa.", "Catering"], ["TN", "Talita N.", "A troca com outras pessoas da área faz cada edição render ainda mais.", "Food service"]];
export default function Testimonials() { const [index, setIndex] = useState(0); useEffect(() => { const id = setInterval(() => setIndex(v => (v + 1) % testimonials.length), 4500); return () => clearInterval(id); }, []); return <section id="depoimentos" className="section testimonials"><div className="container"><Reveal><p className="eyebrow">Vozes da comunidade</p><h2>Ideias compartilhadas<br /><em>viram caminhos.</em></h2></Reveal><p className="section-copy testimonial-note">Depoimentos ilustrativos para representar diferentes perfis de negócio.</p><div className="testimonial-stage">{testimonials.map(([initials, name, quote, role], i) => <article className={i === index ? "testimonial active" : "testimonial"} key={name}><div className="avatar">{initials}</div><div className="stars">{[1, 2, 3, 4, 5].map(n => <Star key={n} size={14} fill="currentColor" />)}</div><blockquote>“{quote}”</blockquote><b>{name}</b><small>{role}</small></article>)}</div><div className="dots">{testimonials.map((_, i) => <button key={i} className={i === index ? "active" : ""} onClick={() => setIndex(i)} aria-label={`Depoimento ${i + 1}`} />)}</div></div></section>; }
