"use client";
import { useEffect, useState } from "react";
import { Star } from "lucide-react";
import Reveal from "./Reveal";
const testimonials = [["MC", "Marina C.", "O Clube me ajudou a sair do improviso e enxergar meu cardápio com olhos de negócio.", "Marmitas"], ["RA", "Rafael A.", "As fichas técnicas deram uma clareza que eu não tinha para organizar minha produção.", "Delivery"], ["JS", "Juliana S.", "Todo mês encontro uma ideia nova que combina com a minha cozinha e com os meus clientes.", "Confeitaria"], ["PL", "Paulo L.", "Hoje eu planejo a semana antes dela começar. Isso mudou o meu caixa e minha tranquilidade.", "Restaurante"], ["BS", "Bianca S.", "O conteúdo é direto, bonito e realmente aplicável na rotina.", "Catering"], ["TN", "Talita N.", "A comunidade virou meu espaço favorito para trocar ideias e continuar evoluindo.", "Food service"]];
export default function Testimonials() { const [index, setIndex] = useState(0); useEffect(() => { const id = setInterval(() => setIndex(v => (v + 1) % testimonials.length), 4500); return () => clearInterval(id); }, []); return <section id="depoimentos" className="section testimonials"><div className="container"><Reveal><p className="eyebrow">Quem já está no clube</p><h2>Feito para quem<br /><em>faz acontecer.</em></h2></Reveal><div className="testimonial-stage">{testimonials.map(([initials, name, quote, role], i) => <article className={i === index ? "testimonial active" : "testimonial"} key={name}><div className="avatar">{initials}</div><div className="stars">{[1, 2, 3, 4, 5].map(n => <Star key={n} size={14} fill="currentColor" />)}</div><blockquote>“{quote}”</blockquote><b>{name}</b><small>{role}</small></article>)}</div><div className="dots">{testimonials.map((_, i) => <button key={i} className={i === index ? "active" : ""} onClick={() => setIndex(i)} aria-label={`Depoimento ${i + 1}`} />)}</div></div></section>; }
